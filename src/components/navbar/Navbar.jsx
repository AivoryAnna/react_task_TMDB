import React from 'react';
import './Navbar.css';

const Navbar = () => {
    return (
        ///* I use the <nav> element because in the original site this section is a navigation block
        <nav className='navbar'>
            <h3>Hello World</h3>
        </nav>
    );
};

export default Navbar;